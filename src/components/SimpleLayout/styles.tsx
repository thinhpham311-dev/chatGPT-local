import tw, { styled, css, theme } from 'twin.macro'

const SimpleLayoutWrapper = styled.section(() => [
    tw` h-dvh relative flex items-center justify-center w-full bg-black`,
])

export { SimpleLayoutWrapper }